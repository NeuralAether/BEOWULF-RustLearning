fn main() {
    // Apparently not println but println! because it is not a normal function but a macro.
    // Macros are like metacoding ! (I need to understand more what it is)
    println!("Hello, world!");
}